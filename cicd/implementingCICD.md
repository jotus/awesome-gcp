

# Install git

sudo yum install git

git config --global user.name "wojciech przechrzta"
git config --global user.email wprzechrzta@gmail.com


- conf keys
ssh-keygen -t rsa -b 4096




- install gradle
add .gradle to .gitignore
>gradle wrapper

> gradle init

- tests
smoke tests/sanity tests - high level integ tests, very basic, is app running, check if partucular endpoint works

- install jenkins
  eg. using yum pkg manager
 -- install suggested plugins
 -- create admin user
 -- setup jenkins projects
   jobs <=> jenkins project






